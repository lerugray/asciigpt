# asciigpt runtime dependencies
# Install: pip install -r requirements.txt

# Image loading and the whole preprocessing stack
# (resize, grayscale, brightness/contrast/sharpness/gamma, invert, threshold).
Pillow>=9.0.0

# FIGlet text banners (--text and banner overlays).
pyfiglet>=1.0.0
