# asciigpt runtime dependencies
# Canonical install is `pip install .` (see pyproject.toml); this file mirrors
# the runtime deps for `pip install -r requirements.txt` workflows.

# Image loading and the whole preprocessing stack
# (resize, grayscale, brightness/contrast/sharpness/gamma, invert, threshold).
# 9.1.0 is the floor: Image.Resampling (used by the resizer) landed there.
Pillow>=9.1.0

# FIGlet text banners (--text and banner overlays).
pyfiglet>=1.0.0
